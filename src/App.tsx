import Kanban from './pages/kanban'

function App() {
  return (
    /*
    TODO:
    * implement save note feature
    * Move note from one list to another
      * Add drag+drop feature to allow this change
    * add or remove lists
    * Add way to have multiple workspaces that hold their own lists
    * make sure that only the people in that worksapce can get the tasks from it    
     */
    <>
      <Kanban />
    </>
  )
}

export default App
